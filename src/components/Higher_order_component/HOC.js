import React, { Component } from "react";

const subjects = (STUDENT) => {

    class subjects extends Component {
        render() {
            return (
                <STUDENT hocSub={"React"} />
            )
        }
    }

    return subjects

}

export default subjects